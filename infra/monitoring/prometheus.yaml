global:
  scrape_interval: 5s

scrape_configs:
  - job_name: infinitecontext
    authorization:
      type: Bearer
      credentials_file: /state/backend.key
    static_configs:
      - targets: ['model:18081']
